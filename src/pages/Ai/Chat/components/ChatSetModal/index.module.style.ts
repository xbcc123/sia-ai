import { createStyles } from 'antd-style';

const useStyles = createStyles(({token}) => {
  return {
    formItemDes: {
      marginLeft: '4px',
      fontSize: '10px',
    },
  };
});
export default useStyles;
